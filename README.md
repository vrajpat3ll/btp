# 5G SCTP loadbalancer

## Pre-requisites

- Check for docker CE compatibility with the OS you're trying to replicate [here](https://docs.docker.com/engine/install/ubuntu/).

```bash
./dock.sh
docker image ls
docker run --rm -ti <image id of the docked image> bash
```